import {Component} from "@angular/core";

// PLACEHOLDER: minimal stand-in so app.routes.ts can route 'form' -> MidrasFormComponent
// before the form engine is ported. Task 12 ports the real component from
// charts6/src/app/form/midras-form.component.ts (+ .html/.scss) and overwrites this file.
@Component({
  selector: 'midras-form',
  template: ''
})
export class MidrasFormComponent {
}
